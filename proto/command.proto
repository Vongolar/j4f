syntax = "proto3";

package command;
option go_package = "data/command;command";

enum command {
    registModule = 0; //注册模块
    log = 1; //输出日志
    runModules = 2;//运行所有模块
    handle = 3; //自动分配一个本地模块执行

    innerMax = 10000; //内部命令最大值
    signup = 10001; //注册
    login = 10002; //登录
}